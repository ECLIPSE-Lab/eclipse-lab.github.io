name: Publication and site refresh

on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * 1" # Mondays 04:17 UTC

permissions:
  contents: write
  pull-requests: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pytest

      - name: Test publication updater
        run: python -m pytest tests/test_generate_publications.py -q

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Regenerate publications
        env:
          SCOPUS_API_KEY: ${{ secrets.SCOPUS_API_KEY }}
          WOS_API_KEY: ${{ secrets.WOS_API_KEY }}
        run: python generate_publications.py

      - name: Refresh citation metrics (OpenAlex)
        run: python fetch_citation_metrics.py

      - name: Render site
        run: quarto render

      - name: Create refresh PR
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.ECLIPSELAB_PR_TOKEN || github.token }}
          commit-message: "chore: automated publication/news refresh"
          branch: automation/publication-refresh
          delete-branch: true
          title: "chore: automated publication/news refresh"
          body: |
            Automated weekly refresh.

            ### What changed
            - Regenerated publication entries from active people profile IDs (`generate_publications.py`)
            - Re-rendered Quarto output (`docs/`) to keep news/publications pages in sync

            ### Why
            Keep website content current with low-maintenance, reviewable updates.
          labels: |
            automation
            content-refresh
